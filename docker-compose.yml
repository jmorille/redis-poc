version: "3.3"
services:
  redis1:
    image: redis
    ports:
       - "6379:6379"
    volumes:
      - "./redis.conf:/usr/local/etc/redis/redis.conf"
    networks:
      - redis-network
    command: [ "redis-server", "/usr/local/etc/redis/redis.conf" ]

  redis2:
    image: redis
    volumes:
      - "./redis.conf:/usr/local/etc/redis/redis.conf"
    networks:
      - redis-network

  redis3:
    image: redis
    volumes:
      - "./redis.conf:/usr/local/etc/redis/redis.conf"
    networks:
      - redis-network

 
networks:
    redis-network:


